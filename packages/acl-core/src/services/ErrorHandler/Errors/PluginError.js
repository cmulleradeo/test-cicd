import MetricBaseError from './MetricBaseError'

/**
 * Generic class used by all the plugins in case of error during its execution
 * or to throw a functionnal error.
 */
export default class PluginError extends MetricBaseError {
}
